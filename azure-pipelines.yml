# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Hosted VS2017

steps:
- task: DownloadSecureFile@1
  inputs:
    secureFile: 'https://modulestorage.blob.bellevue.azurestack.corp.microsoft.com/azaccountscontainer/Az.Accounts.2.1.2.nupkg'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'ls'
    pwsh: true
